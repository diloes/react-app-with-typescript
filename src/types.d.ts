// En este archivo solo puede haber definiciones, por eso el .d.ts Osea solo puede haber
// interfaces, tipos que puedas tener y cosas así.

export interface Sub {
  nick: string
  avatar: string
  subMonths: number
  description?: string
}